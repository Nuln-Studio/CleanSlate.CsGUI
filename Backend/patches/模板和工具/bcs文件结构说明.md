# bcs文件结构说明

CLSL2026-07-28 10:04:37Bohh be46dc64838561d1312e4933aa3dbc7d7276354dde741392ad220d3e29e97976c	 ?  ?     <------这是打包出来的.bcs文件头，它是的格式是二进制，所以你并不能用记事本或普通的文本编辑器直观看到他的内容

CLSL                  -> 这是它的魔数头，只有这个魔数头，才会被白板的主程序识别
(1)                 -> 这是它的版本号，如果是普通的记事本或者文本编辑器，那他会显示乱码
2026-07-28 10:04:37   -> 这是它的打包时间（没啥好说的）
Bohh                  -> 这是它的作者，打包时这里如果留空就会默认是作者大名喽(◦˙▽˙◦)
be46dc64838561d1312e4933aa3dbc7d7276354dde741392ad220d3e29e97976c	-> 喔!好长一串!这是打包时文件的SHA-256哈希值，会以16进制存储在文件中，当然，这是为了后续解包以及加载补丁时校验文件完整性（至于为什么是SHA-256而不是MD5...I don't no...）
 ?  ?                 -> 这是，你猜呢，实际上是python补丁文件的字节长度以及yaml字节长度

|-------------------------------------------------|
|  后面分别是明文的python补丁文件和yaml补丁配置文件  |
|-------------------------------------------------|
例如：
import os
from pathlib import Path

def _get_size_gb(size_bytes):
    return round(size_bytes / (1024 ** 3), 2)

......